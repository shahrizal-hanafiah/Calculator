Time taken: ±10 hours

Description:
Upon receiving string from input, function  Calculate() split the string into array. Then array will be reevaluate using EvaluateArray() function to add start and end statement using '(' as start and ')' as end of statement. EvaluateArray() also split up any parenthesis inside each array item and add it into current arraylist.

After array has been polish, it will loop to process for each object in array. Using the Stack type of collection in C#,operator and values will be store in run time. 

Before exiting the statement, Calculate() will do the calculation based on operator and values stored in Stack at run time.